{
  "name": "@terrarium/webchat-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "poetry run python -m src.main",
    "start": "poetry run python -m src.main",
    "lint": "poetry run ruff check",
    "format": "poetry run ruff format",
    "test": "poetry run pytest"
  }
}
